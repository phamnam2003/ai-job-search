{
  "name": "ashby-cli",
  "version": "1.0.0",
  "description": "CLI for searching jobs across Ashby-hosted company boards via Ashby's public Posting API — no authentication, zero runtime dependencies.",
  "type": "module",
  "main": "src/cli.ts",
  "bin": {
    "ashby-search": "src/cli.ts"
  },
  "scripts": {
    "start": "bun run src/cli.ts",
    "test": "bun test --timeout 30000",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^7.0.0",
    "@types/bun": "latest"
  }
}
