{
  "name": "weworkremotely-cli",
  "version": "1.0.0",
  "description": "CLI for searching remote jobs on We Work Remotely (weworkremotely.com) via its public RSS category feeds — no authentication, zero runtime dependencies. Personal use only.",
  "type": "module",
  "main": "src/cli.ts",
  "bin": {
    "weworkremotely-search": "src/cli.ts"
  },
  "scripts": {
    "start": "bun run src/cli.ts",
    "test": "bun test --timeout 30000",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^7.0.0",
    "@types/bun": "latest"
  }
}
